mixin CardItem({ className, src, width, title, desc, isHiring,link, icon, classCustomIcon, classNumberCountup, nameIcon, isGallery, style })
    .CardItem.horizontal.wow.animate__animated.animate__fadeInRight(class=`${className ? className : ''}`)
        .CardItem-header
            if icon
                .CardItem-header-icon(class=`${classCustomIcon ? classCustomIcon : ''}`)
                    img(src=`${nameIcon}` style=`${style}`)
            if src
                if isGallery
                    a(href=`${link ? link : ''}` data-fancybox="gallery")
                        img(src=`${src}`, width=`${width}`)
                else
                    a(href=`${link ? link : ''}`)
                        img(src=`${src}`, width=`${width}`)
            if isHiring
                .isHiring
                    a(href=`${link ? link : ''}`) By Hr
                    p.isHiring-date 10/26/2022
        .CardItem-body
            block category
            if title
                h3.CardItem-body-title
                    a(href=`${link ? link :''}` class=`${classNumberCountup ? classNumberCountup : classNumberCountup}`)=title
            else
                h3.CardItem-body-title
                    a(href=`${link ? link :''}` ) Mobile Solutions
            if desc
               p.CardItem-body-desc=desc
            else
               p.CardItem-body-desc Thiết kế giải pháp, tư vấn và xây dựng ứng dụng công nghệ di động vào các sản phẩm dịch vụ cho khách hàng.
            block card